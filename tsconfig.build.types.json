{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "emitDeclarationOnly": true,
    "noEmit": false
  },
  // Don't declaration files for core, we hand-type them
  "exclude": ["packages/core/**/*Mixin.js"]
}
